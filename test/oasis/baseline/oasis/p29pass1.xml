doc_start()
handle_doctype(doc, None, None)
dtd_start()
handle_pi(Pi, '')
handle_comment('comment')
new_element_type(doc, EMPTY)
handle_pi(Pi, '')
handle_comment('comment')
new_attribute(doc, att, CDATA, #IMPLIED, None)
handle_pi(Pi, '')
handle_comment('comment')
new_parameter_entity(ent, '')
handle_pi(Pi, '')
handle_comment('comment')
new_notation(not, 'some notation', None)
handle_pi(Pi, '')
handle_comment('comment')
dtd_end()
handle_start_tag(doc, {})
handle_end_tag(doc)
doc_end()
